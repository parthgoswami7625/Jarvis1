services:
  autonomous-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: parths-autonomous-agent
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
